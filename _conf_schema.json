{
  "auto_use_current_name": {
    "type": "bool",
    "description": "自动获取机器人名称",
    "default": true
  },
  "bot_name": {
    "type": "string",
    "description": "自定义机器人名称",
    "hint": "当 自动获取机器人名称 为关闭状态时生效，自定义显示在状态卡片上的机器人名称。",
    "default": "AstrBot"
  },
  "banner_image": {
    "type": "file",
    "description": "自定义状态背景图",
    "default": [],
    "file_types": [
      "png",
      "jpg",
      "jpeg"
    ]
  },
  "traffic_monitor": {
    "type": "object",
    "description": "流量统计",
    "hint": "记录插件运行期间观察到的当月整机网络流量，并可在达到阈值后发送提醒。",
    "items": {
      "enabled": {
        "type": "bool",
        "description": "当月流量统计",
        "hint": "开启后后台每 300 秒采样一次网络计数器，状态卡会显示本月上传/下载累计。",
        "default": true
      },
      "alert_enabled": {
        "type": "bool",
        "description": "流量提醒",
        "hint": "开启后，当本月上传和下载合计达到阈值时，每月最多提醒一次。",
        "default": false
      },
      "alert_threshold_gb": {
        "type": "float",
        "description": "提醒阈值（GB）",
        "hint": "按上传 + 下载合计计算，单位固定为 GB。",
        "default": 100.0
      },
      "alert_target_umo": {
        "type": "string",
        "description": "提醒目标 UMO",
        "hint": "填写 platform_id:message_type:session_id，例如 aiocqhttp:group:123456。留空时不会主动发送提醒。",
        "default": ""
      }
    }
  },
  "enable_llm_analysis": {
    "type": "bool",
    "description": "LLM 分析",
    "hint": "在 /状态 命令返回图片后，是否调用 LLM 分析",
    "default": false
  },
  "vision_provider_id": {
    "description": "识图模型",
    "type": "string",
    "default": "",
    "_special": "select_provider",
    "hint": "用于分析状态图片的视觉模型（需支持图片输入）。留空时优先使用 AstrBot 全局图片描述模型，再回退到当前会话模型。"
  },
  "comment_provider_id": {
    "description": "转述模型",
    "type": "string",
    "default": "",
    "_special": "select_provider",
    "hint": "用于根据识图结果生成文字分析的文本模型。留空时使用当前会话模型。"
  },
  "vision_prompt": {
    "type": "string",
    "description": "识图提示词（发送给视觉模型）",
    "default": "把图片中各种指标用文字描述出来"
  },
  "comment_prompt": {
    "type": "string",
    "description": "转述提示词（发送给文本模型）",
    "hint": "用 {description} 代表识图结果的占位符。",
    "default": "根据以下系统状态描述，用简洁友好的语气总结当前服务器状况，重点关注异常项。\n\n系统状态描述：{description}"
  }
}
